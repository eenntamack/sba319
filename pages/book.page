<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="books.css"/>
    <script>
        document.addEventListener("DOMContentLoaded",()=>{
            let num = 0
            let book = document.body.getElementById("#id#")
            let chapters = #chapters#
            let chapterHolder = document.body.getElementById("chapter")
            chapterHolder.textContent = chapters[num]

            book.addEventListener("DOM)
            for(let i = 0; i<book.length;i++){
                book.addEventListener("onclick",()=>{
                    
                })
            }
        })
    </script>
</head>
<style>
    body{
        height: 100vh;
        width: 100vw;
        margin:auto;
        overflow: hidden;
        position:relative;
    }
    nav{
        display:flex;
        background-color: blueviolet;
        flex-direction: row;
        justify-content: center;
        align-items: center;
        position:relative;
        top:0px;
    }
    ul{
        height: auto;
        width: 100%;
        display: flex;
        flex-direction: row;
        justify-content:space-evenly;
        align-items: center;
    }
    footer{
        width:100%;
        display:flex;
        justify-content: center;
        position:absolute;
        bottom:0px;
        height:30px;
        background-color: blueviolet;
        align-items: center;

    }
    #content {
    background-color: aqua;
    overflow: auto;  /* Only show scrollbars if content overflows */
    height: 70vh;    /* You could also use a fixed height like 500px, or adjust as needed */
    width: 100%;
    position: relative;
    display: flex;
    flex-direction: row;  /* Layout items in a row (horizontally) */
    flex-wrap: wrap;      /* Allow items to wrap to the next line if necessary */
    padding: 10px;        /* Optional padding for spacing */
}
</style>
<body>
    <nav>
        <ul style="list-style:none;">
            <li><a href="">Home</a></li>
            <li><a href="">Items</a></li>
        </ul>
    </nav>
    <section id="content">
        #books#
        <div id="flip"style="background-color:transparent; width:90vw; height: 300px; position: absolute; z-index: 10; display: flex; flex-direction: row; justify-content: space-between; padding:20px; bottom:2fr" >
            <button  type="button" value="left" style="background-color: transparent; border-color: transparent;"><i id="left" class="fa-solid fa-arrow-left" style="position:relative; scale: 4; color:rgba(256,256,256,1);"></i></button>
            <button  type="button" value="right" style="background-color: transparent; border-color: transparent;"><i id="right" class="fa-solid fa-arrow-right" style="position:relative; scale:4; color:rgba(256,256,256,1);"></i></button>
        </div>
    </section>
    <footer>&copy; 2025 Eric Ntamack. All Rights Reserved</footer>
</body>
</html>